type CustomerModel {
    id: ID!
    firstName: String
    lastName: String
    address: AddressModel
}

type AddressModel {
    country: String
    zip: String
    city: String
    streetAddress: String
}

type Query {
    customers(count: Int, offset: Int): [CustomerModel]
}